version: '3.9'

services:

  backend-staging:
    container_name: emp-be-staging
    image: oriontechmen/env-typescript:dev
    ports:
      - "3000:8000"
    volumes:
      - ./.env:/usr/src/app/.env:ro


